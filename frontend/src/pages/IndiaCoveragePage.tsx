import { useEffect, useState } from "react";
import { pilotCities } from "../api/product";
import { ReadinessBadge, ReplayModeBadge } from "../components/product/ProductStates";
export function IndiaCoveragePage(){const [cities,setCities]=useState<Array<{city_id:string;station_ids:string[]}>>([]);useEffect(()=>{void pilotCities().then(x=>setCities(x.data));},[]);return <section className="mx-auto max-w-5xl px-6 py-12"><ReplayModeBadge /><p className="mt-4 text-xs font-semibold uppercase tracking-[.18em] text-air">Coverage</p><h1 className="mt-3 text-3xl font-semibold">India pilot coverage</h1><p className="mt-3 text-sm text-mist">Five validated detailed pilots only. This is not full detailed pan-India coverage.</p><div className="mt-7 grid gap-4 sm:grid-cols-2">{cities.map(c=><article className="border border-line bg-panel p-5" key={c.city_id}><h2 className="font-medium">{c.city_id}</h2><div className="mt-3 flex flex-wrap gap-2"><ReadinessBadge ready label="Forecast replay ready"/><ReadinessBadge ready label="Intelligence replay ready"/></div><p className="mt-3 text-xs text-mist">Stations: {c.station_ids.join(", ")}</p></article>)}</div></section>}
