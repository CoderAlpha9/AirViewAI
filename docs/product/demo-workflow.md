# Demo workflow

Run `python scripts/generate_demo_presets.py` to regenerate curated five-city historical replay presets from existing feature artifacts. The command centre resolves the backend replay timestamp; arbitrary timestamp selection is intentionally unavailable.
