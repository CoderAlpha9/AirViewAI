# Product API overview

`/api/dashboard/demo-presets` supplies filterable curated replays. `/api/orchestration/runs` provides compact paginated audit records and `/api/dashboard/export/{run_id}` supplies JSON or HTML exports.
